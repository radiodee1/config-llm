services:
    client:
        build: client/.
        ports:
            - "5173:5173"
    server:
        build: server/.
        ports:
            - "8000:8000"

        volumes:
            - /home:/home

volumes:
    home:

