services:
    client:
        build: client/.
        ports:
            - "5173:5173"
    server:
        build: express/.
        ports:
            - "8008:8008"

        volumes:
            - /home:/home

volumes:
    home:

